{
  "installCommand": "if [ -d \"website\" ] && [ -f \"website/next.config.ts\" ]; then cd website && npm install; else npm install; fi",
  "buildCommand": "if [ -d \"website\" ] && [ -f \"website/next.config.ts\" ]; then cd website && npm run build; else npm run build; fi",
  "outputDirectory": ".next"
}
